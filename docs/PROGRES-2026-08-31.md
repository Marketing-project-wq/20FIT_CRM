# Progres Kerja — 31 Agustus 2026

Ringkasan pekerjaan pada CRM 20FIT hari ini. Semua di sekitar alur **Campaigns**
(`/campaigns`) — kejelasan nama & pelacakan kampanye, penjadwalan, dan setelan batas kirim.
Railway auto-deploy dari `main` (merge = deploy ke produksi), jadi tiap PR yang di-merge langsung live.

## Ikhtisar PR

| PR | Judul | Status | Deploy |
|----|-------|--------|--------|
| #21 | Configurable send limits + sapuan istilah "campaign" | **Merged** | live |
| #22 | Nama kampanye ke langkah 1, tab "Buat Kampanye", penanda pengiriman lebih jelas | **Merged** | live |
| #23 | Nama kampanye **wajib** + bounce-back dari tab Segmen (draft utuh) | **Disetujui, menunggu merge** | — |
| #24 | Tindak lanjut: lindungi skenario-C, backfill 9 nama legacy, peringatan nama duplikat | **Draft (terbuka)** | — |

## Yang sudah live (merged hari ini)

### PR #21 — Batas kirim bisa diatur
- Tabel singleton `crm_send_config` (migrasi `20260831081344`, diterapkan+verifikasi): `daily_limit` 1000, `workflow_daily_cap` 300.
- Setelan **Settings → "Batas Kirim"**, khusus Super Admin, teraudit ke `crm_audit_log`.
- Batas tersalur ke composer, workflow, dan cron kiriman terjadwal.
- Kenaikan >2× → peringatan reputasi (bukan blokir); rekomendasi ramp sebagai saran; auto-stop bounce 5% tetap aktif dan independen.
- Istilah internal "run" disapu dari layar → "kampanye"/"pengiriman".
- Catatan: kuota paket Mailtrap **tidak terbaca dari kode/API** — pemilik produk cek di dashboard Mailtrap.

### PR #22 — Nama kampanye di awal + penamaan yang dimengerti
- Field **"Nama kampanye"** pindah ke langkah 1 (sebelum pilih segmen).
- Tab `Kirim` → **"Buat Kampanye"** (judul langkah 4 tetap "Kirim").
- Penanda Riwayat Pengiriman diperjelas: asal **"Dibuat manual" / "Dari workflow"**; status **"Terlambat"**, **"Dihentikan"**.

## Yang menunggu (belum di-merge)

### PR #23 — Nama kampanye WAJIB + bounce-back
- Nama kampanye kini **wajib** (validasi client on-blur/submit: trim, 3–100; server tolak `label_required`; resume dikecualikan). Auto-generate nama dilepas dari alur composer.
- **Bounce-back**: klik "Buat segmen baru" menyimpan draft composer ke `sessionStorage` (`20fit:campaign-draft`, berversi) lalu ke tab Segmen; setelah simpan → kembali ke composer, draft ter-restore, segmen baru terpilih otomatis + notice. Tombol "← Kembali ke kampanye" untuk batal.
- **Tidak ada migrasi NOT NULL** di PR ini (ada 8+1 baris legacy NULL) — ditangani di #24.
- **Diverifikasi di browser sungguhan** (bukan stub): 6 skenario (happy path, batal, tombol back browser, buka tab segmen langsung, dua kali kembali, draft rusak) → 23/23 cek lulus, termasuk skenario-C (tombol back browser mempertahankan draft).

### PR #24 — Tindak lanjut (draft)
1. **Lindungi asumsi skenario-C**: komentar eksplisit di titik restore + `campaign-nav.ts` bahwa bounce-back bergantung pada tab = searchParam pada satu route server (unmount→remount memicu restore). Guard `campaign-tab-is-searchparam.test.ts` gagal bila halaman Campaigns berhenti jadi server component berbasis searchParams.
2. **Backfill 9 nama legacy** (file migrasi ada, **BELUM diterapkan** — menunggu review 9 nilai):
   - `crm_campaign_run` 8 NULL, `crm_scheduled_send` 1 NULL → diisi `defaultCampaignLabel` "{segmen} · tanggal" (WIB, bukan ISO), idempoten.
   - Setelah backfill nol-NULL: migrasi terpisah `SET NOT NULL` pada kedua kolom.
   - 9 nilai yang akan dihasilkan sudah ditampilkan ke pemilik produk untuk dicek. Catatan: 6 baris "Testing Team · 28 Agu 2026" identik (bisa ditambah jam WIB agar unik, menunggu keputusan).
3. **Peringatan nama duplikat (lunak, bukan blokir)**: `checkDuplicateCampaignNameAction` — bila nama cocok (case-insensitive, ter-trim) dengan run mana pun dalam 30 hari terakhir, tampilkan garis amber "Nama ini sudah dipakai pada <tanggal>. Kirim tetap bisa dilanjutkan." Dipanggil **on-blur** (satu query per entri, tanpa debounce/library). Tanpa constraint unik DB. Terverifikasi lewat SQL atas data nyata (mendeteksi run "Test").

## Butir menggantung / menunggu keputusan pemilik produk
- **Merge #23** lalu **merge #24** (setelah #23 masuk `main`).
- **Terapkan migrasi backfill #24** setelah 9 nilai disetujui; lalu `SET NOT NULL`; lalu tinjau apakah fallback cron `defaultCampaignLabel` masih perlu (kalau tidak, lepas pemanggilnya).
- **Kuota Mailtrap** — cek di dashboard (tak terbaca dari kode).
- **Sambutan 36 orang** — menunggu pemilik produk memastikan env `CAMPAIGN_SEND_ENABLED` di Railway sebelum Run.

## Gerbang mutu
Untuk semua PR: `tsc` bersih · `eslint` bersih · `vitest` hijau (1243 tes pada #24) · `next build` bersih · guard i18n (coverage/untranslated/stale-phrase) hijau. Tidak ada library baru. Verifikasi browser untuk bounce-back memakai komponen asli + routing Next asli (fixtures menggantikan Supabase; satu langkah tulis-DB disimulasikan lewat URL yang sama dengan redirect `saveSegmentAction`).
